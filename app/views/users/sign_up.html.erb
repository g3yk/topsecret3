<% content_for :head do %>

<%= stylesheet_link_tag 'nicepage' %>
<%= stylesheet_link_tag 'Sayfa-1' %>
<% end %>


<section class="container my-5">
  <div class="row align-items-center">
    <!-- Form Section -->
    <div class="col-lg-6">
      <div class="card shadow p-4">
        <h3 class="text-center mb-4">Kayıt Ol</h3>
        <%= form_with model: @user, url: sign_up_path, class: "signup-form" do |form| %>
          <%= render partial: "shared/form_errors", locals: { object: form.object } %>

          <!-- Username -->
          <div class="mb-3">
            <%= form.label :username, "Kullanıcı Adı", class: "form-label" %>
            <%= form.text_field :username, placeholder: "Kullanıcı Adı Giriniz", class: "form-control", required: true %>
          </div>

          <!-- Email -->
          <div class="mb-3">
            <%= form.label :email, "E-Posta", class: "form-label" %>
            <%= form.email_field :email, placeholder: "Geçerli bir e-posta adresi girin", class: "form-control", required: true %>
          </div>

          <!-- Phone -->
          <div class="mb-3">
            <%= form.label :phone, "Telefon", class: "form-label" %>
            <%= form.telephone_field :phone, placeholder: "Telefonunuzu girin (örn. +14155552675)", class: "form-control", required: true, pattern: "\\+?\\d{0,3}[\\s\\(\\-]?([0-9]{2,3})[\\s\\)\\-]?([\\s\\-]?)([0-9]{3})[\\s\\-]?([0-9]{2})[\\s\\-]?([0-9]{2})" %>
          </div>

          <!-- First Name -->
          <div class="mb-3">
            <%= form.label :first_name, "Adı", class: "form-label" %>
            <%= form.text_field :first_name, placeholder: "Adınızı Girin", class: "form-control", required: true %>
          </div>

          <!-- Last Name -->
          <div class="mb-3">
            <%= form.label :last_name, "Soyadı", class: "form-label" %>
            <%= form.text_field :last_name, placeholder: "Soyadınızı Girin", class: "form-control", required: true %>
          </div>

          <!-- Date of Birth -->
          <div class="mb-3">
            <%= form.label :date_of_birth, "Doğum Tarihiniz", class: "form-label" %>
            <%= form.date_field :date_of_birth, placeholder: "AY/GÜN/YIL", class: "form-control", required: true %>
          </div>

          <!-- Country -->
          <div class="u-form-country u-form-group u-form-group-3">
          <%= form.label :country_id, "Ülke", class: "u-label" %>
          <div class="u-form-select-wrapper">
            <%= form.select :country_id, options_for_select(Country.all.pluck(:name, :id)), { prompt: "Bir ülke seçin" }, id: "user_country_id", class: "u-input u-input-rectangle", required: true %>
            <svg class="u-caret u-caret-svg" version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" width="16px" height="16px" viewBox="0 0 16 16" style="fill:currentColor;" xml:space="preserve">
              <polygon class="st0" points="8,12 2,4 14,4 "></polygon>
            </svg>
          </div>
        </div>

          <!-- Password -->
          <div class="mb-3">
            <%= form.label :password, "Şifre Giriniz", class: "form-label" %>
            <%= form.password_field :password, placeholder: "Şifre", class: "form-control", required: true %>
          </div>

          <!-- Password Confirmation -->
          <div class="mb-3">
            <%= form.label :password_confirmation, "Şifrenizi Doğrulayın", class: "form-label" %>
            <%= form.password_field :password_confirmation, placeholder: "Şifre Doğrulayın", class: "form-control", required: true %>
          </div>

          <!-- Submit Button -->
          <div class="text-center">
            <%= form.submit "Kayıt Ol", class: "btn btn-primary w-100" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Image Section -->
    <div class="col-lg-6 d-none d-lg-block">
      <%= image_tag "foto-register.jpg", class: "img-fluid rounded", alt: "Register Image" %>
    </div>
  </div>
</section>
